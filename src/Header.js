import React from 'react';

const Header = (props) => {
    return (
        <div id='header' className="jumbotron">
          <h1>GDAX Ticker</h1>
	        <h2>Tracking cryptocurency exchange rates</h2>
        </div>
    );
  };

export default Header;
